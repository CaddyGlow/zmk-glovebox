keyboard: no_firmwares
description: Test keyboard with no firmware configurations
vendor: Test Vendor
version: v1.0.0

# Flash configuration
flash:
  method: mass_storage
  query: vendor=Test and removable=true

# Build configuration
build:
  method: docker
  docker_image: test-zmk-build
  repository: test/zmk
  branch: main

# No firmwares section

# Templates
templates:
  keymap: |
    #include <behaviors.dtsi>
    #include <dt-bindings/zmk/keys.h>
    {{ resolved_includes }}